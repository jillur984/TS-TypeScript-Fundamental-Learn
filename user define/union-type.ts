let userId: string | number | boolean;

userId="jillur rahman"
userId="10"
userId=true

function displayUserInfo(userId:string | number | boolean){
    userId
}

let res=displayUserInfo(101)
console.log(res);
